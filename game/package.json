{
  "name": "neon-drift-runner",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "esbuild src/main.js --bundle --minify --format=iife --outfile=dist/game.js"
  },
  "dependencies": {
    "esbuild": "^0.20.0",
    "three": "^0.160.0"
  },
  "devDependencies": {
    "playwright": "^1.48.0"
  }
}
